{% extends 'base.html.twig' %}

{% block title %}Hello EmployeController!{% endblock %}

{% block body %}
    <h1>Liste des employés</h1>
    {% for employe in employes %}
        <p><a href="{{ path('show_employe', {'id': employe.id})}}">{{employe}}</a> ({{ employe.entreprise.raisonSociale}})</p>
        <a class="btn btn-warning" href="{{path ('edit_employe', {'id': employe.id})}}">Edit</a>
        <a href="{{ path('delete_employe', {'id': employe.id})}}" class="btn btn-danger">Supprimer</a>
    {% endfor %}
{% endblock %}
